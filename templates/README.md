# NixAI Configuration Templates

This directory contains JSON descriptors and NixOS configuration templates for use in the Visual Configuration Builder and REST API.

- Add new components by creating a `component.json` and corresponding `.nix` file.
- The builder and API will automatically detect and load all templates in this folder.
