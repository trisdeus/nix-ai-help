# test-plugin Plugin Instructions

This plugin was generated using the 'basic-go' template.

## Next Steps

- 1. Navigate to the plugin directory
- 2. Run 'go mod tidy' to download dependencies
- 3. Build the plugin with 'go build -buildmode=plugin -o plugin.so .'
- 4. Install the plugin with 'nixai plugin install plugin.so'
- 5. Test your plugin with 'nixai plugin execute test-plugin hello'
